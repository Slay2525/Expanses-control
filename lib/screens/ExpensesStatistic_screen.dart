import 'package:flutter/material.dart';

class ExpensesStatisticsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Expenses Statistics'),
      ),
    );
  }
}
